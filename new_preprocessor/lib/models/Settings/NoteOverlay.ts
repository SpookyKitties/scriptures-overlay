export class NoteOverlayNew {
  public name: string;
  public className: string;
  public overlay: number;

  public constructor(name: string, className: string, overlay: number) {
    this.name = name;
    this.className = className;
    this.overlay = overlay;
  }
}
